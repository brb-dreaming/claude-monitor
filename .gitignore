# Compiled binary
agent_monitor
claude_monitor
.build/
__pycache__/
*.pyc

# Runtime session data
sessions/
monitor.sock
agent_monitor.lock
claude_monitor.lock
layer_debug.txt
codex_notify_chain.json
codex_notify_chain.json.loop-backup

# Internal reviews
reviews/

# macOS
.DS_Store

# Local-only scratch
.obsidian/
logos/

# Live config (personal settings, voice IDs, etc.)
# config.default.json is the tracked template — copied to config.json on first build
config.json
config.json.personal
